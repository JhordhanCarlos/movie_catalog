import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    margin: 20,
  },
  searchBar: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    backgroundColor: "#FFF",
    borderColor: "#544e4eff",
    borderWidth: 1,
    width: "auto",
    margin: 10,
    paddingHorizontal: 10,
    borderRadius: 10,
  },
});
